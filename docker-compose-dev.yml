version: "3.8"

services:
  cups:
    build: .
    image: drcoccodrillus/cups-filters:dev
    container_name: cups-filters
    restart: always
    privileged: true
    networks:
      - printer-network

networks:
  printer-network:
    name: printer-network
    driver: bridge
